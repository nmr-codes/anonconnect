version: "1"

services:
  # Managed Redis Instance
  - type: redis
    name: anonconnect-redis-bp
    region: ohio
    plan: free
    ipAllowList: [] # empty means only internal access

  # The FastAPI Backend
  - type: web
    name: anonconnect-backend
    env: docker
    dockerContext: backend
    dockerfilePath: backend/Dockerfile
    region: ohio
    plan: free
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: anonconnect-db-bp
          property: connectionString
      - key: REDIS_URL
        fromService:
          type: redis
          name: anonconnect-redis-bp
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: FRONTEND_URL
        value: "https://anonconnect-alpha.vercel.app"
      - key: DEBUG
        value: "False"
      - key: GOOGLE_CLIENT_ID
        value: "500724399689-1jkl9pmjv2qu775108qev1n279d5mjga.apps.googleusercontent.com"

databases:
  # Managed PostgreSQL Database
  - name: anonconnect-db-bp
    region: ohio
    plan: free
